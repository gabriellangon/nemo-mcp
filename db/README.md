# Database Assets

This folder is public and versioned on purpose.

- `schema.sql` is the base schema for fresh deployments
- `migrations/` contains forward-looking or optional migration scripts

For a new setup, start with `schema.sql`.
